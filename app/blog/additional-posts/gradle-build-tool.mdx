# Gradle Build Tool

1. `build.gradle` = pom.xml
2. `gradle run` - run the application, need gradle installed on your machine
3. `gradlew.bat run` - run the application, don't need gradle installed on your 
machine


