---
title: 'Data Structures & Algorithms: Design HashMap'
publishedAt: '2026-01-14'
---

`HashMap` is a data structure that stores `(key, value)` pairs. The big deal and why it comes up so 
often is that **insert**, **lookup**, and **delete** operations are all `O(1)` on average.

Many programming problems involve repeatedly searching for information we have already encountered, and 
a HashMap allows us to remember that information and retrieve it quickly instead of performing the same s
earch again. This idea of trading additional memory for faster lookup forms one of the most useful 
problem-solving patterns in DSA.

<HashMapConceptVisualizer />

> **Playground**: [Leetcode 706](https://leetcode.com/problems/design-hashmap/description/)

---

### Functional Requirements

Implement the `MyHashMap` class:

1. `MyHashMap()`: Initializes the object with an empty map.

2. `void put(int key, int value)`: Inserts a `(key, value)` pair into the `HashMap`. If the key already 
exists in the map, update the corresponding value.

3. `int get(int key)`: Returns the value to which the specified key is mapped, or `-1` if this map 
contains no mapping for the key.

4. `void remove(key)`: Removes the key and its corresponding value if the map contains the mapping for 
the key.

---

### Non-Functional Requirements

1. Keys should be unique.

2. Should support concurrent operations.

---

### Design Rationale

Understanding how a `HashMap `works internally helps us not only use it effectively but also understand 
why it provides fast lookups and how such data structures are designed.

##### **<span className="text-emerald-800">Q1. Why a Hash Function matters?</span>**

##### **<span className="text-emerald-800">Q2. How to handle collisions?</span>**

##### **<span className="text-emerald-800">Q3. Explain bucket/index calculation.</span>**

##### **<span className="text-emerald-800">Q4. Explain chaining vs. open addressing.</span>**

##### **<span className="text-emerald-800">Q5. Explain average vs. worst case lookup.</span>**

##### **<span className="text-emerald-800">Q6. How resizing/load factor works.</span>**

---

### Object-Model Design

### Follow-Up Questions

Q1. How concurrentHashMap is implemented?

How can we compare hashMaps? Why the frequency map of two anagrams are the same?

```
Map<Character, Integer> frequencyMap = str.chars()
            .mapToObj(c -> (char) c)
            .collect(Collectors.groupingBy(Function.identity(), Collectors.counting()));
        anagramMap.computeIfAbsent(frequencyMap, k -> new ArrayList<>()).add(str);
```
