---
title: 'Data Structures & Algorithms (Backtracking): Combination Sum'
publishedAt: '2025-10-01'
---

Given an array of distinct integers `candidates` and a target integer `target`, return a list of all unique 
combinations of candidates where the chosen numbers sum to `target`. 

> **NOTE:** You may return the combinations in any order. The same number may be chosen from `candidates` an unlimited number of times. Two combinations are unique if the frequency of at least one of the chosen numbers is different.

```
Example 1:

Input: candidates = [2,3,6,7], target = 7
Output: [[2,2,3],[7]]
Explanation:
2 and 3 are candidates, and 2 + 2 + 3 = 7. Note that 2 can be used multiple times.
7 is a candidate, and 7 = 7.
These are the only two combinations.

Example 2:

Input: candidates = [2,3,5], target = 8
Output: [[2,2,2,2],[2,3,3],[3,5]]

Example 3:

Input: candidates = [2], target = 1
Output: []
```

> **Playground:** [Leetcode 39](https://leetcode.com/problems/combination-sum/)

---

### Key Observations

For every candidate, we can either include it in the combination (if including it keeps the combination sum less than
or equal to target), or exclude it and move to the next candidate.



> **NOTE:** Ordering is not important in combinations. For example, `[2,2,3]` is same as `[3,2,2]` and `[2,3,2]`.




 



---

### Approach 1: Using Recursion

